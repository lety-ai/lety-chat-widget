{
  "name": "@lety-ai/widget-loader",
  "version": "0.0.0",
  "private": true,
  "description": "Lightweight loader script that injects the Lety chat widget into a host page",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"TODO: eslint\" && exit 0",
    "test": "echo \"TODO: tests\" && exit 0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
