{
  "name": "lety-widget",
  "version": "0.0.0",
  "private": true,
  "description": "Lety embeddable chat widget: loader script, iframe chat app, and React component package",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10.12.1"
  },
  "packageManager": "pnpm@10.26.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
